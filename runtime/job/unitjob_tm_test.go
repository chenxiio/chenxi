package job

import (
	"testing"
)

func TestUnitjobTM(t *testing.T) {
	// tm := UnitjobTM{
	// 	tmactions: []*TMAction{},
	// 	cond:      sync.NewCond(&sync.Mutex{}),
	// }
	// a1 := TMAction{Priority: 1, Arm: "action1", MoveAction: &MoveAction{id: "action1"}}
	// a2 := TMAction{Priority: 2, Arm: "action2", MoveAction: &MoveAction{id: "action2"}}
	// a3 := TMAction{Priority: 3, Arm: "action3", MoveAction: &MoveAction{id: "action3"}}

	// go func() {
	// 	tm.StartExecution()
	// 	tm.RemoveAction(&a1)
	// }()

	// // 添加动作
	// tm.AddAction(&a1)
	// go func() {

	// 	tm.RemoveAction(&a2)
	// }()
	// tm.AddAction(&a2)
	// go func() {

	// 	tm.RemoveAction(&a3)
	// }()
	// tm.AddAction(&a3)

	// for {
	// 	time.Sleep(time.Hour)
	// }
	// 等待执行完成
	// 这里可以使用sync.WaitGroup或其他方式等待执行完成

	// 删除动作

	// 等待执行完成
	// 这里可以使用sync.WaitGroup或其他方式等待执行完成
}
