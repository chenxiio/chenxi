package runtime

// func WafersPostions(list []string) ([]string, error) {
// 	uniqueLetters := make(map[string]bool)
// 	var firstLetters []string
// 	for _, str := range list {
// 		if len(str) > 0 {

// 			unitp, err := chenxi.CX.IOServer.ReadString(context.TODO(), "env.wp."+str)
// 			if err != nil {
// 				return nil, err
// 			}

// 			if !uniqueLetters[unitp] {
// 				uniqueLetters[unitp] = true
// 				firstLetters = append(firstLetters, unitp)
// 			}
// 		}
// 	}
// 	return firstLetters, nil
//
